# 更新日志

每个版本简短记一句改了什么。**发版时，本文件里对应版本(`## v版本号`)的条目会被 CI 自动取出，
作为 GitHub Release 说明 + 应用内「发现新版本」弹窗里显示的更新内容**（见 `.github/workflows/release.yml`）。
所以发版前，先在最上面加一条本版条目。

## v0.3.7
- 素材侧栏「文件夹」改成**可折叠的目录树**：导入一个大文件夹后,它会作为父节点出现(哪怕它本身没有直接图片),点箭头展开看里面的子文件夹
- **删文件夹现在级联**：删一个文件夹会连同它下面**所有子文件夹**的素材一起从库移除(原文件不动),不用再一个一个删;仍是点两下确认防误删
- 点父文件夹 = 看它名下**所有**素材(含各级子文件夹);点子文件夹只看该层

## v0.3.6
- 修复：玩游戏时(尤其骗子酒馆/UNO)**就算把聊天窗藏起来、也没有任何新消息,任务栏图标还一直闪**——主窗的未读提醒以前把游戏同步帧也当成"新消息"了;现在它会跳过游戏帧,只有真聊天才提醒/闪
- 改进：游戏流量(开房/加入/对局同步)现在**彻底不写进聊天记录**了(全改走实时广播 + 房主每 3 秒重广播房间,让晚到的人照样能加入)。根治"玩几局之后聊天记录被刷没/看不到之前消息"
- 内部：修掉 src/App.tsx 里一个隐藏的空字节(会让该文件被当成二进制),零行为改变

## v0.3.5
- 修复（0.3.4 引入的回归）：**开房后别人加入不了**——上一版把所有游戏帧都改走了"不留记录"的实时广播,导致晚一步打开面板的人收不到"房间"那条、看不到加入按钮。现在开房/加入这类帧改回走可回放的通道(晚到/重连也能进),只有高频的对局同步走广播(聊天历史依旧不被刷掉)。UNO / 飞行棋 / 骗子酒馆都修了
- 修复：游戏「退出」后约 15 秒,若触发"房主掉线接管",仍可能被重新拉回房间——这次把退出拦截补全到房间帧,退出后任何重广播都不再拉回

## v0.3.4
- 修复：玩了 UNO / 飞行棋 / 骗子酒馆后**看不到之前的聊天消息**——游戏同步流量过去会被当成消息写进记录、把最近 50 条聊天挤掉；现在游戏帧改走实时广播、不再写进消息历史，聊天记录干净了
- 修复：游戏面板的「退出」现在**真的退得干净**，不会再被房主每隔几秒的心跳同步几秒后拉回牌桌（UNO / 飞行棋 / 骗子酒馆都改了）
- 修复：玩游戏时**任务栏异常闪烁 / 冒出乱码消息**——多为房间里有人还在旧版导致；新增「控制字符开头的帧一律不进聊天流」兜底，并请大家都升级到最新版
- 内部：去重表加上限，长时间挂着不再缓慢吃内存

## v0.3.3
- 新游戏「🍷 骗子酒馆」（说谎者扑克）：群聊工具栏点酒杯开局，2–4 人
  - 一副 20 张牌：A/K/Q 各 6 张 + 2 张 Joker（百搭，算任意点）；每轮发 5 张、随机定一张桌面牌
  - 轮到你只能二选一：盖着打出 1–3 张并声称「都是桌面牌」（真假随你诈唬），或喊「骗子!」质疑上家刚打的牌；没牌时只能质疑
  - 摊牌：上家的牌全是桌面牌或 Joker → 质疑者输；有一张不符 → 出牌的骗子输
  - 输的人对自己开一枪（俄罗斯轮盘）：每人一把左轮、6 膛随机 1 发实弹，扣过越多次越险（1/6→1/5→…）；中弹出局，最后活着的人赢
  - 复用 UNO 那套房主权威同步：房主掉线可接管、断线重连不卡界面

## v0.3.2
- UNO 新增 5 张「削牌」功能牌，专治手牌只进不出、越打越厚（都是四色万能、随时可出，用完连同烧掉的牌洗回牌堆，不进弃牌堆/不改色）：
  - 🔥 燃烧：选一种颜色，把手里该色的牌一次烧掉（最多 4 张）——攒了一堆同色时爆发
  - 🔁 连击：选一个点数，把手里该点的牌一次清掉（最多 4 张）——攒了一堆同点时爆发
  - ♻️ 净化：随便弃掉 2 张，无条件、随时能用的稳定小削
  - 🎲 豪赌：押 1–4 张并摇骰，点数大于押注张数就烧掉、否则反而倒摸等量真牌——押得越多越险
  - 🫳 甩锅：自己挑最多 4 张直接塞给下家（自己至少留 1 张）——又削牌又坑下家
- 以上削牌牌（燃烧/连击/净化/豪赌）若把手牌清空即直接获胜；甩锅是"给牌"不算获胜

## v0.2.26
- UNO 质疑/替换卡住兜底：有人迟迟不表态/不操作时，约 30 秒后房主自动按默认结果收尾（质疑→视为"不质疑"、替换→放弃），不再死等
- UNO 房主掉线可接管：长时间收不到房主同步时，提示"房主可能掉线"，由下一位玩家一键「接管房主」让对局继续（不必整局重开）
- UNO 中途刷新/重连不再花屏：有对局在进行时不会再把"开一局"页和牌桌叠在一起

## v0.2.25
- 修复 UNO 质疑/替换等多人同时操作时，个别玩家漏收一次同步就卡在界面、点击没反应的问题：房主每 3 秒重广播一次权威状态，漏收的端会自动追上进度

## v0.2.24
- 游戏弹幕加表情：玩 UNO / 飞行棋时，底部弹幕输入条左边多了 😊 表情按钮，和聊天框同一套 emoji
- 聊天气泡颜色改为广播：你选的气泡颜色现在会随消息发给大家，别人也能看到你的颜色（聊天气泡 + 游戏弹幕都生效）。⚠️ 房主需把聊天设置里的 Supabase 初始化 SQL 再跑一次（幂等，只是给消息表加一列 bubble）才会生效

## v0.2.23
- UNO 新增电击牌：牌堆里藏着 4 张电击牌，谁摸到就直接多摸 2 张；电击牌本身洗回牌堆继续循环（不会出现在起手牌、不可出）
- UNO 新增闪电牌（仿三国杀）：可随时当变色牌打出，但不进弃牌堆，而是挂在自己头上；论一圈后每轮到被挂者就判定一次，命中(随机约 1/6)→该玩家摸 4 张、闪电消失，未命中→顺位移到下家头上。场上同时只存在一片闪电
- UNO 新增王炸牌：牌堆里 2 张，可随时打出，打出后除自己外每个玩家各摸 4 张；王炸牌用完洗回牌堆继续循环（不进弃牌堆、不改色）
- UNO 新增四色反转牌：4 张「变色版反转」，可随时打出、出时选色，效果同反转（2 人局当再出一张），让反转更常见
- UNO 新增侵袭牌：6 张普通数字牌带红色⚔角标，打出后其他人按顺序轮流应招——手里有同色或同点的牌就必须打出一张（不触发其功能），没有则自动摸 2 张；全部应招完从侵袭者下家继续正常出牌
- UNO 新增替换牌：4 张四色万能牌，打出后从下家手里盲翻 2 张（只出牌人可见），留 1 张进自己手、塞自己 1 张给下家（双方手牌数不变），也可放弃；替换牌用完洗回牌堆
- UNO 新增质疑牌：4 张四色万能诈唬牌，打出时声称「手里有同色或同点的牌」，其余玩家各自点质疑/不质疑——声称属实则质疑者摸 1，是诈唬则出牌人摸 1（按质疑人数各算一次）；质疑牌用完洗回牌堆

## v0.2.22
- UNO 新增炸弹牌：牌堆里藏着 6 张炸弹，谁摸到就摇骰子、几点多摸几张（不会出现在起手牌）
- UNO「摸到能出为止」：出不了牌时一直摸到摸出能打的牌（或牌摸光）
- UNO 回合倒计时：每回合 30 秒，超时只闪烁提醒（不替你出牌、不踢人）

## v0.2.21
- UNO 加入 +2/+4 叠牌：被罚时可接同类把累计罚牌甩给下家，接不住一次摸完
- UNO 牌桌更直观：按当前色染桌、放大「要压的牌」、轮到谁金色高亮、手牌自动排序 + 计数、谁剩 1 张高亮 ⚠UNO
- 游戏内弹幕聊天：玩 UNO / 飞行棋时底部可直接打字，消息以弹幕飘过，不用退出游戏

## v0.2.20
- 浏览窗彻底隐身：按 Alt+3 调出标题栏也不再露到任务栏/Alt+Tab，全程除老板键外任何方式都看不见（和便签一致）

## v0.2.19
- 便签改为置顶悬浮（和浏览窗一样浮在其它窗口最上层）

## v0.2.18
- 修复便签的「隐藏 / 最大化」和浏览窗 Alt+3 的窗口按钮只闪一下就消失（之前窗口一获焦就被打回隐身样式）；现在隐身只在首次打开时设一次，不再打断后续操作

## v0.2.17
- 聊天里能玩飞行棋了：右上角 🎲 开局，朋友点加入即可对战（2–4 人，掷 6 起飞、撞子回家、四子到家获胜）
- （本版飞行棋为基础玩法，暂不含「飞」和捷径跳跃，后续再加）

## v0.2.16
- UNO：修复打完一局后卡在结算页、开不了下一把——结算页给所有人加「退出」，房主可「再来一局」；出牌后即时显示「出牌中…」反馈，按钮/卡牌有按下感
- 便签：透明度首次打开就生效了（之前要点一下窗口才变透明）
- 便签：标题栏补上「隐藏 / 最大化」按钮（隐藏后用老板键 Alt+C 或托盘唤回）
- 浏览窗：按 Alt+3 显示标题栏时带上完整的最小化 / 最大化 / 关闭（此时会临时出现在任务栏），收起标题栏恢复隐身

## v0.2.14
- 聊天：可改自己的气泡颜色（头像面板里选，8 个预设 + 自定义取色；字色自动适配深浅）
- 聊天里能玩 UNO：右上角 🎴 开局，朋友点加入即可对战（2–4 人）
- 画板：从桌面拖图到画板上现在直接落在画板上（临时、不入库）；想存进素材库可在画板里右键图片「保存到素材库」

## v0.2.0
- 内置翻译大升级：划词翻译支持多语种；英文查词走离线词典（音标 + 释义，约 3.7 万常用词随包，断网也能查）
- 整句可离线翻译：内置神经翻译模型（中英互译），有本地大模型时优先用、没有或断网也能翻；模型首次使用时按需下载
- 技术词（python3、ai_config 等）不再被乱翻成「蟒蛇」；桌面划词 / 应用内 / 浏览器扩展三处统一显示音标 + 释义
- 新增「和朋友聊天」：默认走云端，支持并排开多个群、桌面拖图直接发送、Alt+C 老板键一键隐藏所有聊天窗

## v0.1.15
- 素材库侧栏精简：去掉配色分类、标签默认只列常用的（冷门收进「全部标签」）、智能合集收成小按钮
- 网格新增 图片 / 视频 / 音频 快捷筛选；所有筛选都支持「再点一下取消、回到全部」
- 移除 3D 预览（本机环境无法稳定渲染），安装包瘦身

## v0.1.14
- 看球小窗快捷键可自定义（逐个改键 + 冲突检测 + 恢复默认）
- 老板键：藏窗时自动暂停视频、显示时自动续播
- 弹窗按钮样式统一

## v0.1.13
- 看球小窗重做：以「直开网页」为主，登录 / 全屏 / 站内跳转都正常
- 一排快捷键（透明度 / 缩放 / 穿透 / 换台搜索 / 截图入库 / 贴角 / 老板键）
- 记住窗口大小位置、透明度、搜索引擎
