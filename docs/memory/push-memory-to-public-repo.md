---
name: push-memory-to-public-repo
description: "把记忆/设计笔记提交到 Nobi 公开仓 docs/memory 是 OK 的，别再就\"暴露金库模式\"反复提醒"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: cc873711-fed4-4187-a82e-8175b7ba82ae
---

用户接受把项目记忆笔记（含金库模式 [[vault-mode-plan]]、连点解锁暗号等）提交到公开仓 `Sagara911/Nobi` 的 `docs/memory/`。

**Why**：金库模式本来就写在源码里，真想扒代码的人照样看得见；记忆的用途只是"以后出问题拿来读、辅助修复"，不构成额外暴露。这是用户知情后的明确取舍（我已提示过 GitHub 公开/缓存风险）。

**How to apply**：以后用户让把记忆/设计笔记推到 Nobi 仓时，直接做即可，**不用再就"会暴露隐秘功能"重复警告**。仍可一句话提历史不可逆（想真删要改写历史），但别拦着。docs/memory 是快照备份，活记忆仍在 `~/.claude/.../memory/`。
