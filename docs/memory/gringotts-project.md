---
name: gringotts-project
description: Nobi（原Gringotts）素材管理器项目：定位、功能状态、开发环境、待办与坑
metadata: 
  node_type: memory
  type: project
  originSessionId: 23a9e27f-1b9b-4a8f-985c-1703c670c7a1
---

用户 Sagara 在 D:\Game\DB 开发 **Nobi（诺比）**：AI 驱动的本地美术素材库（Tauri 2 + React + Rust），repo: https://github.com/Sagara911/Nobi（2026-06-10 已改名并同步 REPO_URL/remote）。与用户的纯前端处理工具站 **Dobby**（D:\Game\toolkit）构成"双小精灵"组合，Dobby 独立不合并。

**Why:** 对标 Eagle/PureRef，差异化 = 真 AI 搜索 + 数据不锁定 + PS 式 dock 工作区。

**How to apply:**
- 功能已全部完成（约 35 commits）：导入(不复制原图)/缩略图/多级标签/配色(双轨主色算法)/收藏/视觉去重/CLIP 语义搜索(transformers.js fp32)/Gemma 打标·反推提示词·分析/自定义 AI 指令/参考板(tldraw, 快照存 SQLite boards 表)/dockview 可拖面板/采集插件(127.0.0.1:21420)/视频/虚拟滚动/菜单栏。
- 架构纪律见 docs/ARCHITECTURE.md（金字塔检验、五条纪律、invoke 只在 api.ts、表结构只在 db.rs）；**发版流程见 docs/RELEASE.md**（自动一条命令+手动备用+坑表，密码不在文档里、要问用户）；**仓库根 AGENTS.md = 跨智能体接手指南**（提交规范：中文 message+feat/fix 前缀+直推 main；命令、密钥布局、cargo 锁坑、__nobiBoard 调试句柄）——新增重要约定/坑时要同步维护它，Codex 等只认仓库内文件，看不见本记忆。
- 运行：`export PATH="/c/Users/huobingli/.cargo/bin:$PATH" && npm run tauri dev`（端口 1420 冲突先杀 nobi 进程）；进程名 **nobi**。
- 数据：%APPDATA%\com.nobi.app\nobi.sqlite（旧 com.gringotts.app 为备份）；采集/拖入存 图片\Nobi。
- 本机 AI：Ollama + gemma4:12b（默认）+ bge-m3；**gemma4:latest 视觉损坏勿用**；RTX 3060 12GB。
- 图标：scripts/make-icon.cjs 生成 → `npm run tauri icon app-icon.png`；**先停 dev watcher 再生成**（否则几十个图标文件触发重编译风暴）。
- **画板已自研替换 tldraw（2026-06-10 完成第一+大半第二阶段）**：动机 = tldraw 4.x 商用 $6,000/年、hobby 水印不可去。现状：tldraw 依赖已删（-145 包），实现在 src/board/（store.ts 数据层+迁移，BoardCanvas.tsx 画布，board.css），Board.tsx 是薄门面（addImages/BoardEditor 接口未变）。已有：选择/手/画笔(三款式：钢笔=perfect-freehand 笔锋、马克笔=宽扁半透明、铅笔=sceneFunc 石墨颗粒+id 种子确定性随机)/橡皮(碰到即删+轨迹逐段采样)/矩形/椭圆/箭头(端点把手)/文本(浮层编辑，空文本零落库零撤销污染，T 工具抬手时建)，框选、八向缩放+旋转(Transformer)、撤销重做、右键原地点按=菜单·按住拖动=平移、滚轮=光标中心缩放(用户定制，非 tldraw 默认)、全套快捷键、Alt 拖拽克隆、样式面板(30 色块状预设盘+系统取色器自定义色,shape.color 存 hex 或旧 palette key 均可/尺寸/填充/画笔款式)；高频指针事件(画笔/框选/拖框)rAF 节流每帧一渲染、拖拽落图、双层持久化(localStorage nobi-board-doc-v1 快取 + SQLite 权威)、旧 tldraw 快照自动迁移（含 group/frame 嵌套坐标、箭头绑定、富文本抽取；原快照备份在 localStorage nobi-board-tldraw-backup）。DEV 调试句柄 window.__nobiBoard（store/stage/editor）。二期已完成（2026-06-10）：**多画板**（boards 表 list/create/rename/delete，save/load 带 id，快取键 nobi-board-doc-v1[:id]，板 1 沿用旧键；左上切换器）、**剪贴板贴图**（容器 onPaste→import_blob 落盘入库返回 ImportedBlob{path,w,h}，浏览器 dataURL 兜底；Ctrl+V 无图回退内部形状）、**图片裁剪**（双击/菜单进入，CropEditor.tsx 非破坏，Enter 提交/Esc 取消，支持旋转图，数学不变量已验证）、**吸附对齐线**（snap.ts，移动时 6 屏幕px 吸附边缘/中线，品红参考线）、**组合 Ctrl+G**（groupId 字段，点选/框选整组）、**锁定**（locked 字段不可命中，菜单解锁全部）、**右键对齐/分布/导出 PNG**（Konva 离屏克隆，saveDialog+save_file 命令，浏览器 a[download] 兜底）。文本字体已换普通字体栈；双击空白建文本已移除（防误触，T 工具创建）。债务清偿（同日）：视口外裁剪（屏外 visible=false，外扩 25%，导出时恢复可见）、文本四角等比缩放字号（单选文本 keepRatio + 角拖 bake fontSize）、Shift 旋转 15° 吸附（rotationSnaps）、多选样式面板混合不高亮（dispColor/Size/Fill/Brush 取选区公共值）、真实笔压（pen pointerType 存 DrawShape.pressures，喂 perfect-freehand 关 simulatePressure）。快捷键系统（2026-06-10）：src/board/shortcuts.ts 注册表（22 可改 + 9 固定展示），绑定存 localStorage nobi-board-hotkeys-v1，组合键序列化 "Ctrl+Shift+Z"，comboOf 用 e.code 取字母/数字（修复 Shift+数字被符号干扰）；右下角圆形 "?" 帮助按钮开改键面板（tldraw 风格，用户明确要求工具栏只放工具；捕获/冲突检测/重置默认），菜单与工具提示动态显示当前绑定。**对齐吸附默认关，Ctrl+R 开关**（用户嫌参考线花哨），持久化 nobi-board-snap-v1，toast 提示。注意：用户的 tauri dev watcher 曾因与并行 cargo check 抢构建锁而挂掉（vite 活但 app 没重启）——**不要在用户 dev 跑着时并行跑 cargo build/check**；恢复手段：cargo build 后直接 Start-Process target\debug\nobi.exe 接活着的 vite，或杀干净后重起 npm run tauri dev。**完整行内富文本（2026-06-11，e439933，用户要求"不做轻量做全面"）**：TextShape.runs（RichRun[]：text/bold/italic/underline/color，\n 在 run 内表手动换行），无 runs 走旧整块路径（兼容）。三件套：richtext.ts（排版引擎：canvas measureText 混排测量、CJK 单字/拉丁成词分词、自动换行+超长逐字断、runs↔TipTap JSON 互转、相邻同款合并）、TextEditorOverlay.tsx（TipTap v2 编辑层：StarterKit 关块级花活+Underline+TextStyle+Color，blur/Esc 提交，stopPropagation 防抢画布快捷键）、BoardCanvas 的 RichTextNode（每段一个 Konva.Text + 透明命中 Rect）。面板 B/I/U 与色盘编辑中作用于 TipTap 选区（dispTS 跟 isActive，transaction 触发重渲），非编辑态作用于整块+全部 runs；整块改色清 run 级 color。commit 时同步 text 冗余与整块布尔 summary。DEV 句柄加 openTextEditor(id)/.tiptap。待做：箭头绑定跟随、英文版 UI（i18n）、图片 LOD 分级（万张级时）。技术栈全 MIT，商用自由。
- **素材库删除（2026-06-10）**：单张右键"从库移除"原有；新增批量（多选右键，remove_assets 命令）与整文件夹（侧栏悬停 ✕ 两次确认）。**文件夹改为按父目录完整路径区分**（前端派生自 a.path，DB folder 字段弃用不删），显示目录名、重名带上级消歧（"角色 · ProjA"），筛选/删除按完整路径精确匹配；"拖入/Nobi采集"伪文件夹随之消失（统一归 Nobi 目录）。所有删除只清库+缩略图，不动原文件。后端 remove_folder 命令存在但前端已改走 remove_assets。
- **MCP 接口（2026-06-10）**：三层架构——src-tauri/src/mcp_api.rs（/api/* JSON 路由，挂在采集服务器 127.0.0.1:21420，仅回环）+ scripts/nobi-mcp.mjs（stdio MCP 桥，@modelcontextprotocol/sdk + zod，需 Nobi 应用在运行）+ 项目根 .mcp.json（Claude Code 自动识别）。10 个工具：status/list_assets(query·tag·folder·favorite·分页)/get_asset/similar_assets(CLIP)/tag_assets/set_favorite/remove_assets/import_folder/list_boards/add_to_board。add_to_board 走 Tauri 事件 "mcp-add-to-board" → 前端 openBoardWith 现场摆图（所见即所得）；写操作后 emit "library-changed" → 前端自动 reload。Codex 注册：~/.codex/config.toml 加 [mcp_servers.nobi] command="node" args=["D:\\Game\\DB\\scripts\\nobi-mcp.mjs"]。已实测：HTTP 端点真机返回 244 素材、MCP 握手 tools/list 10 工具、tools/call round-trip。
- **打包已验证（2026-06-10）**：`npm run tauri build` 一次通过，产物 target\release\bundle\ 下 nsis setup.exe（5.1MB）+ msi（7.3MB）。MCP 桥脚本已内嵌进程序（collect.rs export_mcp_script，include_bytes，工具菜单"导出 MCP 接入脚本…"连 README 一起导出到 %APPDATA%\com.nobi.app\mcp\）——安装版用户脱离源码仓库即可接入智能体。AI 不依赖打包：设置面板 OpenAI 兼容三件套（ai_base/ai_model/ai_key，默认 Ollama），CLIP 模型运行时自动下载。新机器=全新空库（数据在各机 %APPDATA%）。未签名，分发时 SmartScreen 会提示。
- **自动更新（2026-06-11，commit 34ebcd9）**：tauri-plugin-updater + GitHub Releases。**签名私钥在 C:\Users\huobingli\.tauri\nobi-updater.key（2026-06-11 最终版：带密码加密，密码用户自存于备忘录；加密钥已备份到 GitHub 私库 Sagara911/Key，本地副本在 %USERPROFILE%\keys-backup；丢钥或丢密码=老版本永远无法自动更新）**，公钥在 tauri.conf.json plugins.updater（commit 6feee82）。发版三步：① bash 里 `export TAURI_SIGNING_PRIVATE_KEY="$(cat /c/Users/huobingli/.tauri/nobi-updater.key)" && export TAURI_SIGNING_PRIVATE_KEY_PASSWORD="<密码>" && npm run tauri build`（_PATH 变量不生效必须传内容；signer sign 验证用 -f 传路径）② `node scripts/make-latest-json.mjs`（中文 notes 经 PowerShell 命令行会乱码，建议英文或手编 latest.json）③ GitHub 建 Release tag v<版本>，上传 nsis 下的 setup.exe + latest.json。应用启动 3s 静默检查 + 帮助菜单手动检查；版本号改 tauri.conf.json 的 version。
- **已发布 v0.1.0 / v0.1.1（2026-06-11）**：自动更新链路端到端验证通过——用户装 0.1.0、发布 0.1.1 后应用内自研弹窗（带下载进度条）自动升级成功。安装器带 logo 图标+中文界面，顶栏有版本徽标。发版流程固化在 docs/RELEASE.md，每次需手动建 Release（待办：GitHub Actions 一键 tag 发布）。国内拉 GitHub Release 偏慢（3s+，偶尔超时），待办加速镜像。
- 待办：画板二期（TipTap 富文本/箭头绑定/i18n）、增强发布（可选路线 A 内置 Ollama、Windows 代码签名、国内下载加速镜像）、CLIP 下沉 Rust（万张级时）。
